pymupdf==1.28.2
psycopg[binary]==3.2.13
python-dotenv==1.2.2
fastapi==0.139.0
uvicorn[standard]==0.51.0
transformers==5.15.0

# torch is NOT installed from PyPI here -- the default wheel is CPU-only. For the
# GTX 1050 Ti (sm_61) install the CUDA build explicitly:
#
#   scripts/pip-install.sh torch --index-url https://download.pytorch.org/whl/cu124
#
# Use that wrapper rather than calling pip directly: the wheel is 2.5 GB and pip
# stages downloads in %TEMP% on C:, which has under 1 GB free. The failure mode is
# "OSError: [Errno 28] No space left on device" while E: still shows 380 GB free.
# torch==2.6.0+cu124
